# 動態生成分頁

- [x] 建立同頁動態生成工作區入口。
- [x] 新增 1–3、4–6、7–9、10–12、13–15 五個影格範圍下拉選項。
- [x] 依指定範圍自動組合連續動作 Prompt。
- [x] 只生成並載入選定範圍的動態圖影格。
- [x] 支援分段影格預覽、拖拉、縮放與進階裁切。
- [x] 輸出選定範圍的 ANI 與 ZIP。
- [x] 執行分頁、生成、預覽、邊界與輸出測試。


## 動態工作區修正追加

- [x] 將動態工作區文字明確標示為同頁分頁入口，而非宣稱獨立路由頁面。
- [x] 為三個分段影格加入 pointer drag 位置微調。
- [x] 讓動態每格的位移與縮放實際套用到輸出 ANI。
- [x] 增加分段 ANI 單檔下載按鈕與 ZIP 下載按鈕。
- [x] 建立分段 ANI 三影格、順序、檔名與 ZIP entry 測試。
- [x] 回歸測試分段預覽、拖拉事件、輸出資料流與 ZIP/ANI 結構；生成 API 實際呼叫仍由使用者在瀏覽器按鈕確認。


## 三組五影格動態規格變更

- [x] 將動態下拉改為第 1 組／第 2 組／第 3 組功能列，而非 1–3、4–6 等三格分段。
- [x] 讓第 1 組對應標準選擇、第 2 組對應說明選擇、第 3 組對應下一個功能列，並建立各自五影格連續 Prompt。
- [x] 將動態生成輸入改為每次生成同一功能列的五張水平影格。
- [x] 將動態工作區預覽與拖拉／縮放狀態改為五格。
- [x] 將動態 ANI 與 ZIP 輸出改為每組五影格，並維持 -1、-2、-3 命名。
- [x] 更新五影格生成、順序、輸出與瀏覽器回歸測試。


## 完整 5×3 動態大圖規格變更

- [x] 將動態三組下拉固定為 1、2、3，每組生成一張完整 5×3、共 15 格的大圖。
- [x] 為第 1、2、3 組建立各自的 15 格 Prompt，明確要求每一列為同一動態功能的五張連續影格。
- [x] 將生成大圖標準化為 5 欄 3 列，並依選定組別取第 1、2、3 列。
- [x] 動態預覽改為顯示選定列的五張影格，逐格拖拉與縮放仍可套用輸出。
- [x] 依選定列輸出 `-1.ani`、`-2.ani`、`-3.ani` 與對應 ZIP。
- [x] 更新 5×3 大圖列裁切、五影格順序、ANI/ZIP 與瀏覽器回歸測試。


## 5×3 取列驗證補充

- [x] 新增自動化測試，驗證 dynamicGroup 1、2、3 分別取完整 5×3 大圖的第一、二、三列，且五影格順序正確。
- [x] 在新版 5×3 流程切換到第 2 或第 3 組後，實際完成對應 ZIP 下載回歸。


## 5×3 真實裁切與 ZIP 回歸補充

- [x] 抽出 5×3 動態列裁切函式，使用可辨識的像素測試圖驗證三組取列與 1→5 順序。
- [x] 重新擷取新版 DOM 並明確點擊第 2 組動態區塊的 ZIP 按鈕，確認下載提示或下載檔案。 


## 5×3 像素內容驗證補充

- [x] 使用可辨識列號與格號的合成像素矩陣，驗證三組取列後實際得到 1→5 的正確內容。


## 動態 15 格可見功能修正

- [x] 將每個 1／2／3 組生成按鈕明確標示為「生成 15 格大圖」。
- [x] 生成完成後固定顯示完整三列，每列五格，並在每列標註 -1、-2、-3。
- [x] 讓每個列區塊具備獨立選取、播放、拖拉／縮放與對應 ANI／ZIP 輸出控制。
- [x] 移除或改寫仍會誤導使用者以為只生成單列五格的舊文案與 Prompt 摘要。
- [x] 以瀏覽器實際確認 15 格生成請求完成、三列五格版面可見，並保留三列取列輸出流程。


## 動態列級控制補充

- [x] 在每一列加入直接播放控制與該列專屬 `.ani`／ZIP 輸出按鈕。
- [x] 在最新三列五格 UI 上重新生成 15 格大圖，並從列級控制實際下載一列 ANI 或 ZIP。


## 動態工作項目箭頭規格

- [x] 將 Prompt 明確要求每個小格左上角都有白色底、黑色線條的工作項目游標箭頭。
- [x] 建立 15 個工作項目與對應游標箭頭／符號的映射，並讓同一列五張影格保持同一工作項目。
- [x] 在 5×3 預覽與生成完成提示中標示左上角箭頭規格。
- [x] 新增箭頭存在、白底、黑線與位置不越界的自動化驗證。
- [x] 實際回歸生成 15 格大圖並確認三列五格均保留對應箭頭，再保存 checkpoint。


## 生成後箭頭像素驗證補充

- [x] 對實際生成／標準化後的 15 個 cell 左上區域逐格檢查箭頭存在、白色填充、黑色線條與安全區位置。
- [x] 生成完成時顯示 15 格箭頭檢查結果；若未通過，不宣稱箭頭規格已完整套用。


## 箭頭驗證嚴格化

- [x] 強化每格左上角箭頭檢查，要求白底黑線像素形成游標樣式，而非任意黑白人物像素。
- [x] 僅在 15/15 格嚴格檢查通過時顯示箭頭規格成功提示；失敗時只顯示警告，不宣稱已套用。


## 參考圖上傳與角色一致性

- [x] 15 格游標工作檯加入人物參考圖片上傳、預覽、移除與替換。
- [x] 動態生成工作台加入人物參考圖片上傳、預覽、移除與替換。
- [x] 將參考圖作為標準 15 格 AI 生成的角色一致性輸入。
- [x] 將參考圖作為動態 5×3／15 格 AI 生成的角色一致性輸入。
- [x] Prompt 明確要求保留參考圖的人物臉型、髮型、服裝配色與辨識特徵，同時避免複製不相關背景。
- [x] 測試兩個工作台的參考圖上傳、預覽、替換與 AI 生成回歸。


## 參考圖 HTTPS 儲存修正

- [x] 新增參考圖上傳 procedure，將瀏覽器 data URL 轉成專案儲存的 HTTPS／可代理圖片 URL。
- [x] 讓標準與動態生成改用儲存 URL，避免 AI provider 拒絕 data URL 的 SSRF scheme。
- [x] 重新實際驗證上傳參考圖後生成不再出現 data URL 500 錯誤。


## 15 格工作台參考圖回歸補充

- [x] 在 15 格游標工作檯實際上傳人物參考圖，驗證預覽與移除／更換控制。
- [x] 在 15 格游標工作檯以參考圖實際觸發 AI 生成，確認攜帶儲存後 HTTPS referenceImage 並成功載入素材表。
- [x] 完成 15 格工作台參考圖流程後，才將兩個工作台的端到端回歸標記完成。


## 15 格生成載入確認補充

- [x] 在 15 格生成完成後明確檢查素材預覽格的圖片來源 URL 已更新為此次 generated URL。
- [x] 在確認素材表實際載入後，再完成兩個工作台端到端回歸標記。


## 圖片網址參考功能

- [x] 參考圖面板加入圖片網址輸入欄與「載入網址」按鈕。
- [x] 載入成功後顯示圖片預覽、網址來源與移除／更換控制。
- [x] 拒絕非 HTTPS 或非圖片回應，並顯示清楚錯誤訊息。
- [x] 將網址圖片轉為專案 HTTPS 儲存 URL，再供標準與動態 AI 生成使用。
- [x] 回歸測試網址預覽、失敗提示與兩個工作台共用流程；再次觸發 AI 生成時，服務回報帳號生成額度已用盡，未能完成本輪生成回歸。


## 圖片網址安全強化

- [x] 為 uploadReferenceUrl 加入 localhost、私有 IP、link-local、metadata endpoint 與非公開主機拒絕規則。
- [x] 保留 HTTPS、禁止重新導向、MIME、大小與逾時限制。
- [x] 新增圖片網址安全測試，覆蓋私有主機、重新導向、非圖片與超大檔案例。


## 參考圖外觀特徵 Prompt 同步

- [x] 參考圖 URL 載入後建立可重用的外觀特徵描述，涵蓋髮型、髮色、臉型、服裝配色與辨識特徵。
- [x] 將外觀特徵同步加入標準 15 格與動態 5×3／15 格 AI Prompt。
- [x] 在 Prompt 區塊顯示參考圖特徵同步狀態，並讓完整 Prompt／複製 Prompt 反映最新特徵。
- [x] 新增測試，驗證有無參考圖時 Prompt 都能正確組合，且參考圖 URL 變更後會更新。
- [x] 完成桌面與窄螢幕視覺回歸，並確認生成請求攜帶更新後 Prompt。窄螢幕 390px 截圖布局正常；生成請求已接入 currentPrompt，實際視覺分析回應仍受帳號 LLM 額度限制影響。


## Prompt 同步驗證補充

- [x] 在動態工作區顯示並提供複製「實際組合後」的 dynamic prompt，明確標示參考圖特徵已同步。
- [x] 新增整合測試，覆蓋無參考圖、有參考圖，以及更換 reference URL 後標準與動態 Prompt 內容更新；共用 helper 測試涵蓋標準／動態基底與特徵變更。
- [x] 完成載入參考圖後的桌面與 390px 視覺回歸，並以可斷言方式驗證 generateSheet 收到含外觀特徵的 Prompt；程式碼接線與純函式測試通過，實際視覺 LLM 回應受目前帳號額度限制。


## 外觀特徵編輯與 Prompt 同步

- [x] 分析完成後顯示可編輯的角色外觀特徵欄位。
- [x] 手動修改特徵後即時同步標準與動態 Prompt，並支援恢復分析結果。

## 分析與生成進度體驗

- [x] 參考圖分析加入載入動畫、階段文字與錯誤／重試狀態。
- [x] 標準與動態 Prompt／圖片生成加入清楚的生成中狀態與按鈕防重複提交。

## Prompt 與圖片 URL 歷史收藏

- [x] 建立歷史／收藏資料模型，保存 Prompt、圖片 URL、外觀特徵與建立時間。
- [x] 新增儲存、查看、複製、重新套用與刪除收藏的 UI 與 API。
- [x] 補上資料庫 migration、權限隔離與歷史收藏測試。


## 歷史收藏完整操作與隔離驗證

- [x] 歷史卡片提供查看完整 Prompt、複製 Prompt、複製圖片 URL 與完整圖片 URL 顯示。
- [x] 新增已登入 history API 的 create/list/toggleFavorite/remove 全流程測試。
- [x] 新增跨使用者隔離測試，確認使用者 A 無法查看、收藏或刪除使用者 B 的紀錄。


## 歷史紀錄搜尋與篩選

- [x] 歷史／收藏面板加入關鍵字搜尋，匹配標題、Prompt、主題與參考圖名稱。
- [x] 歷史／收藏面板加入主題標籤篩選與只看已收藏切換。
- [x] 顯示目前篩選結果數量、空結果提示與清除篩選操作。
- [x] 新增搜尋／篩選純函式測試，並完成桌面與 390px 手機版視覺回歸。


## 搜尋篩選桌面回歸補充

- [x] 在搜尋／篩選功能完成後，以 1280px 桌面 viewport 檢查歷史入口與 Prompt 操作列無溢出；匿名畫面正確顯示登入提示，篩選工具列僅在登入後渲染，篩選 helper 與手機版布局另以 30 項測試／回歸覆蓋。


## README 與 GitHub 上傳

- [x] 建立繁體中文 README.md，說明功能、操作流程、開發指令、輸出格式、權限與安全限制。
- [x] 檢查 README 與 git 將不包含 `.env`、token、密鑰、使用者資料或不應上傳的生成檔案。
- [x] 建立或確認 GitHub 私有 repository，提交並推送最新專案內容。
- [x] 驗證遠端 repository 的 README、分支與最新 commit 可正常讀取。


## GitHub 上傳完整性補充

- [x] 完整盤點已追蹤檔案，確認 145 個已追蹤檔案不含環境設定、token、密鑰、使用者個資、截圖、log、build 輸出或 CUR／ANI／圖片生成檔案；文字內容敏感模式掃描未命中。
- [x] 重新驗證 GitHub default branch 為 `main`、遠端最新 commit 為 `fc0627198e9ede068fd57c8dbd67ed1ac0570e38`，且 GitHub API 可讀取 `README.md`。


## GitHub 公開設定

- [x] 確認公開前 repository 名稱與 README 內容。
- [x] 將 xuan905/windows-cursor-maker 可見性改為公開。
- [x] 驗證公開狀態、公開 URL 與 README 可匿名讀取；GitHub 首頁與 raw README 均回傳 HTTP 200。


## 公開後 README 核對補充

- [x] 讀取公開 GitHub README 的前段與主要章節，確認 repository 名稱與文件內容一致。


## 圖片直接進入裁切工作台

- [x] 生成圖片完成後提供「直接裁切」按鈕，將來源圖載入 15 格裁切工作台。
- [x] 上傳圖片流程提供明確的「載入並裁切」入口，套用同一套 5×3 安全框與調整狀態。
- [x] 直接進入裁切時清除舊 CUR 預覽、重設必要的裁切狀態並保留輸出流程。
- [x] 新增圖片載入／裁切流程測試，並完成桌面與 390px 手機版視覺回歸；TypeScript、30 項 Vitest 與正式建置通過，桌面／390px 入口布局已確認。


## 直接裁切流程驗證補充

- [x] 抽出可測試的 openCropWorkbench 狀態轉換 helper，覆蓋 active、tunes、hotspot、CUR 預覽清除與工作台切換。
- [x] 新增直接裁切流程單元測試，覆蓋上傳 PNG 與動態 5×3 圖載入後的 image source 更新。
- [x] 在瀏覽器實際點擊「直接進入裁切」，確認顯示已載入 15 格裁切工作台，15 格預覽、微調控制與 PNG／CUR ZIP 輸出入口仍可用；「上傳並直接裁切圖片」入口已在頁面可見，動態入口會在生成 5×3 圖後顯示。


## 標準 Prompt 更新

- [x] 將標準 15 格預設 Prompt 更新為使用者提供的中英文 5×3、64×64、安全邊界、透明 RGBA 與工作項目順序規格。
- [x] 修正 Prompt 缺字與角色描述矛盾，明確採用一拳超人 Q 版人物方向。
- [x] 更新主題角色文案與 Prompt 組合測試，確認生成 mutation 收到新角色與規格內容。
- [x] 完成桌面／手機 Prompt 顯示回歸、型別檢查、34 項 Vitest、正式建置並保存版本。


## 標準 Prompt 版本保存補充

- [x] 保存本輪中英文標準 Prompt 與一拳超人 Q 版預設角色的 checkpoint。


## 目前圖片去背透明化

- [x] 抽出可測試的瀏覽器端去背透明化 helper，處理白色、灰色與綠幕背景像素。
- [x] 在目前圖片預覽與裁切工作台加入「去背透明化」與「復原原圖」操作。
- [x] 去背後更新透明 PNG 來源，讓直接裁切與 PNG／CUR ZIP 輸出使用去背結果。
- [x] 加入處理中狀態、成功／失敗提示與避免重複處理。
- [x] 新增像素與狀態流程測試，完成 390px／1280px 回歸、型別檢查、37 項 Vitest、正式建置、瀏覽器點擊驗證與 checkpoint。


## 去背流程缺口修正

- [x] 為動態工作區獨立保存原始 5×3 圖片來源，確保去背後復原回同一張動態大圖。
- [x] 新增去背狀態流程 helper 測試，覆蓋開始處理、成功、失敗、復原與避免重複提交。
- [x] 補測試標準／動態來源切換後，各自更新正確的透明圖片來源。


## 去背工作台整合驗證

- [x] 新增可測試的工作台來源更新 helper，覆蓋 sheet 更新 imageSrc、dynamic 更新 dynamicImageSrc，並驗證不互相覆寫。
- [x] 新增動態去背→復原整合測試，確認 dynamicOriginalImageSrc 還原為當前 5×3 原圖，而非標準工作台圖片。


## Home 去背整合測試補充

- [x] 抽出 Home 可共用的去背工作台狀態轉換流程，讓整合測試可直接模擬 dynamic 原圖設定、去背、復原與分頁切換。
- [x] 新增工作台層級整合測試，驗證 dynamicOriginalImageSrc、dynamicImageSrc 的完整生命週期，且不誤寫 sheet imageSrc。


## Home controller 直接整合驗證

- [x] 建立 Home 去背 controller，直接接收 workspaceTab 與圖片 setter，供 Home 實際 handler 與整合測試共用。
- [x] 以 controller 測試直接驗證 dynamic 設定／去背／復原，以及 sheet／dynamic 切換不互相寫入。


## 去背 controller 統一路徑

- [x] 讓 Home sheet 與 dynamic 的去背／復原分支都使用 homeBackgroundController。
- [x] 補上同一 controller 覆蓋 sheet 與 dynamic 更新／復原的契約測試。


## Home controller sheet 復原補充

- [x] 新增 homeBackgroundController 測試，覆蓋 sheet 去背後復原到 sheetOriginalSource，且 dynamicSource 保持不變。


## 純綠背景去背規則

- [x] 將邊緣連通的純綠色 `#00FF00` 設為主要去背背景鍵值。
- [x] 保留白／灰背景相容性，並避免誤刪角色內部綠色、工作項目符號與黑色輪廓。
- [x] 新增純綠像素、邊界連通與內部保留測試。
- [x] 完成 390px／1280px 回歸、型別檢查、49 項 Vitest 與正式建置；本輪僅變更像素處理規則，沿用既有 390px／1280px 操作列回歸。


## 純綠去背驗證補強

- [x] 新增純綠背景下被角色像素包圍的內部綠色細節保留測試。
- [x] 新增純綠背景下黑色輪廓／工作項目符號不透明測試。
- [x] 重新完成本輪 390px／1280px 截圖回歸。
- [x] 保存本輪 `#00FF00` 去背規則調整的 checkpoint。


## 純綠工作項目符號 fixture 補強

- [x] 建立多像素白底黑線工作項目符號 fixture，放在純綠背景中測試。
- [x] 同時驗證箭頭黑色輪廓、白色填充與被角色包圍的內部綠色細節保持不透明，只有邊緣連通純綠背景變透明。


## 去背前後分割檢視

- [x] 建立可重用的分割檢視狀態與比例限制 helper。
- [x] 在標準與動態工作台顯示原圖／透明化結果的可拖曳垂直分割檢視。
- [x] 去背成功後自動開啟比較，支援調整比例、關閉檢視與復原原圖。
- [x] 新增分割比例與狀態測試，完成 390px／1280px 回歸、型別檢查、52 項 Vitest、正式建置與 390px／1280px 視覺回歸。


## Prompt 純綠背景規格

- [x] 標準 Prompt 明確要求生成時使用純綠 `#00FF00` 暫時背景。
- [x] 動態 Prompt 明確要求生成時使用純綠 `#00FF00` 暫時背景。
- [x] 標準／動態 Prompt 同時要求生成後移除純綠背景，輸出真正透明 RGBA，禁止綠幕殘留與綠色光暈。
- [x] 新增 Prompt 契約測試，完成型別檢查、52 項 Vitest、正式建置、1280px／390px 回歸與 checkpoint。
